;;; init-java.el --- Java configuration  -*- lexical-binding: t; -*-

;;; Code:

;; Java uses the built-in java-mode.
;; Eglot with jdtls provides LSP support if needed:
;;   (add-to-list 'eglot-server-programs '(java-mode "jdtls"))

(provide 'init-java)
;;; init-java.el ends here
