;;; init-yaml.el --- YAML configuration  -*- lexical-binding: t; -*-

;;; Code:

;; yaml-mode is now available on MELPA — no need to build from source
(use-package yaml-mode)

(provide 'init-yaml)
;;; init-yaml.el ends here
