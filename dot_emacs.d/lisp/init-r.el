;;; init-r.el --- R / ESS configuration  -*- lexical-binding: t; -*-

;;; Code:

;; ESS is now available on MELPA — no need to build from source
(use-package ess)

(provide 'init-r)
;;; init-r.el ends here
